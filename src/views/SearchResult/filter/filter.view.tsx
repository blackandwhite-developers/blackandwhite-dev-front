import React from "react";
import styles from "./filter.view.module.scss";
import cn from "classnames/bind";

const cx = cn.bind(styles);

const Filter = () => {
  return <div> 필터 페이지임</div>;
};

export default Filter;
